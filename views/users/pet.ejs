<body
  class="bg-image p-4 text-center shadow-2-strong  mb-6 text-white"
  style="background-image: url('/images/pink.png');"
>
<div class="text-center mb-3">
<h3>We've found thousands of matches in your area.</h3>
<h3>And they all happen to be in the market for an owner. </h3>
<h3>Click on their names to get their info!</h3>

<ul>
  <div class="text-center mb-3" style="list-style: none;">
    <% animals.forEach(animal => { %>
   
       <li>
        
        <img class="rounded" src= "<%= (animal.photos[0]) ? animal.photos[0].medium : 'https://place.dog/300/200' %>"/>
        <h1><a class="fas fa-bars text-light text-decoration-none" href="<%= animal.url %>"><%= animal.name %></a></h1>
        <p><%= animal.gender %></p>
        <p><%= animal.age %></p>
        <p><%= animal.size %></p>
        <p><%= animal.tags[0] %></p>
      </li>
        <form id="form" method="POST" action="/users/favorites">
         <input hidden value="<%= animal.name %>" name="name"/>
         <input hidden value="<%= animal.photos[0] ? animal.photos[0].medium : 'https://place.dog/300/200' %>" name="photos"/>
         <input hidden value="<%= animal.age %>" name="age"/>
         <button type="submit" class="btn btn-outline-light btn-lg m-2"> Like </button>
      </form>
  <% }) %>
</ul>
</div>
</body>